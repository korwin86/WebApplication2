<!DOCTYPE html>
<html>
<head>
    <link type="text/css" rel="stylesheet" href="~/Content/Site.css" />

    <title></title>
</head>
<body>
    <div>

        @Html.ActionLink("На главную", "Index", "Home")

        <br />

        @Html.ActionLink("Покупатели", "Purchases", "Home")

        <br />

        @RenderBody()

    </div>

    <footer>
        @if (IsSectionDefined("Footer"))
        {
            @RenderSection("Footer")
        }
        else
        {
            <span> (c) Футер по умолчанию </span>
        }
    </footer>
</body>
</html>
